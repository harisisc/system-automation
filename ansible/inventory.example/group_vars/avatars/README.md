Drop `<username>.png` here to give that account a login/profile picture
(see `../../../README.md#user-avatars`).

In the real `inventory/` (copied from this example directory, not
tracked in git) this directory holds actual photos and is gitignored.
It's kept here, empty but for this file, just so the directory structure
carries over when you copy `inventory.example/` to `inventory/`.
