---
- name: Require Ubuntu hosts for this role
  ansible.builtin.assert:
    that:
      - ansible_distribution == "Ubuntu"
    fail_msg: "This role currently supports Ubuntu only."

- name: Install KVM, QEMU, and GNOME Boxes packages
  ansible.builtin.apt:
    name: "{{ kvmhosts_packages }}"
    state: present
    update_cache: true
